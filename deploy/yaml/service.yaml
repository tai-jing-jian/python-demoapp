apiVersion: v1
kind: Service
metadata:
  name: python-demoapp
  namespace: demoapp    
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  selector:
    app: python-demoapp